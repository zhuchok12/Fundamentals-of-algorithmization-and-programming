#ifndef RECURSIVELY_INVERT_H
#define RECURSIVELY_INVERT_H

class Recursively_invert
{
public:
    Recursively_invert();
    int invert(int &number, int reversed_number = 0);
    bool containsZero(int n);
};

#endif // RECURSIVELY_INVERT_H
